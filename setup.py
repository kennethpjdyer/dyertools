
from setuptools import setup

setup(
    name="dyer",
    version="2019.1",
    packages=['libdyer'],
    #package_data={'dyer':['data/Makefile', 'data/*.xsl', 'data/*.sty']},
    scripts=['bin/dyer'])
        
